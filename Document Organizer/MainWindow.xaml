<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Document_Organizer.MainWindow"
        Title="MainWindow" Height="700" Width="1100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="Files" Grid.Row="1" SelectionChanged="ShowSelectionFromFiles"/>
        <TreeView x:Name="ORM" Grid.Row="1" SelectedItemChanged="ShowSelectionFromDB"  Grid.Column="1"/>
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition  Height="Auto"/>
                <RowDefinition />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0">File</Label>
            <Label x:Name="SelectedFile" Grid.Row="0" Grid.Column="1" />

            <Label Content="Name" Grid.Row="1"/>
            <TextBox x:Name="FriendlyName" Grid.Row="1" Grid.Column="1" Margin="5"/>

            <Label Grid.Row="2" Content="Folder"/>
            <ComboBox x:Name="Folder" DropDownOpened="UpdateFoldersCombobox" Grid.Row="2" Grid.Column="1" Margin="5"/>

            <Grid Grid.Row="4" Grid.Column="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Click="DeleteEntry" Content="Delete" Margin="5" Padding="4"/>
                <Button Grid.Column="2" Click="SaveEntry" Content="Save" Margin="5" Padding="4"/>
            </Grid>
        </Grid>
    </Grid>
</Window>