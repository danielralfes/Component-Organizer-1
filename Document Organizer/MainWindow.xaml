<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:OrganizerDB;assembly=OrganizerDB"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Document_Organizer.MainWindow"
        Title="Component Organizer" Height="500" Width="900">
    <Window.Resources>
        <HierarchicalDataTemplate DataType="{x:Type local:Part}">
            <TextBlock Text="{Binding Path=PartName}" />
        </HierarchicalDataTemplate>

        <HierarchicalDataTemplate DataType="{x:Type local:Manufacturer}" ItemsSource="{Binding Parts}">
            <TextBlock Text="{Binding Path=Name}" />
        </HierarchicalDataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="150"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <GridSplitter Grid.Column ="1" Width="4" Background="Transparent"
                      HorizontalAlignment="Left" VerticalAlignment="Stretch"/>

        <TreeView x:Name="ORM" Grid.Row="1" Grid.Column="0" />

        <DockPanel Grid.Row="1" Grid.Column="2" DataContext="{Binding ElementName=ORM, Path=SelectedItem}">
            <Grid DockPanel.Dock="Top">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="1">Name</Label>
                <TextBox Text="{Binding Path=PartName}"  Grid.Row="1" Grid.Column="1" Margin="5"/>

                <Label Grid.Row="3">Datasheet</Label>
                <Grid Grid.Row="3" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Name="DatasheetTextbox" Margin="5" Text="{Binding Path=Datasheet.FileName}" LostFocus="TextBox_LostFocus_1" GotFocus="TextBox_GotFocus_1"></TextBox>
                    <Button Name="BrowseDatasheet" Grid.Column="1" Margin="5" Padding="2" Click="BrowseDatasheet_Click_1">Browse</Button>
                </Grid>

                <Label Grid.Row="4">Distributer URL</Label>
                <TextBox Text="{Binding Path=DistributorURL}" Grid.Row="4" Grid.Column="1" Margin="5"></TextBox>

                <Label Grid.Row="5">Manufacturer</Label>
                <ComboBox x:Name="Manufacturer" SelectedItem="{Binding Path=Manufacturer}" DropDownOpened="UpdateManufacturersCombobox" Grid.Row="5" Grid.Column="1" Margin="5"/>

                <Label Grid.Row="6">Manufacturer URL</Label>
                <TextBox  Name="blah" Text="{Binding Path=ManufacturerURL}" Grid.Row="6" Grid.Column="1" Margin="5" />

                <Label Grid.Row="7">Inventory</Label>
                <Grid Grid.Row="7" Grid.Column="1" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width=".5*" />
                    </Grid.ColumnDefinitions>
                    <xctk:IntegerUpDown Value="{Binding Path=QuantityAvailable}" FormatString="N0" Increment="1" Minimum="0" DefaultValue="0" Grid.Column="1" Margin="5"></xctk:IntegerUpDown>
                </Grid>
            </Grid>

            <Grid DockPanel.Dock="Bottom">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Row="1" Grid.Column="0" Click="DeleteEntry" Content="Delete" Margin="5" Padding="4"/>
                <Button Grid.Row="1" Grid.Column="2" Click="SaveEntry" Content="Save" Margin="5" Padding="4"/>
            </Grid>
        </DockPanel>
    </Grid>
</Window>